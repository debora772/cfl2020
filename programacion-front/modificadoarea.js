let readlinesync = require("readline-sync");
let base = readlinesync.questionInt("indique la base:");
console.log("base");
let altura = readlinesync.questionInt("indique la altura:");
console.log("altura");
let area = readlinesync.questionInt("base * altura:");
console.log(area);