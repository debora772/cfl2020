let rl= require("readline-sync");
let texto= rl.question("ingrese el texto");
let arregloTexto= texto.toLowerCase();

function textoEntero(arregloTexto){
    for(let i=0; i<textoEntero.length; i++);

};
function textoAlreves(arregloTexto){
    for(let textoAlreves=0; i>0; i--);
};

function textoEntero(arregloTexto){
    if(textoEntero==textoAlreves){
        return true;
    }else{
        return false;
    }
    
};
console.log(arregloTexto);
console.log(textoEntero(arregloTexto));
